---
phase: 11
plan: 09
type: test
wave: 3
depends_on: [11-02, 11-03, 11-04, 11-05, 11-06, 11-07, 11-08]
files_modified: []
autonomous: true
requirements: [YEAR-01, YEAR-02, YEAR-03, YEAR-04, DATE-01, DATE-02, DATE-03, DATE-04]
---

# Plan 09: Full Test Suite Verification (Wave 3)

## Objective

Run the complete test suite to verify all 7 renderer implementations are correct, all 8 test files pass, and there are no regressions in the existing test suite. This plan has no file modifications — it is a verification gate that confirms all prior waves are complete and correct.

## Tasks

<task id="11-09-01">
<type>test</type>
<files></files>
<read_first>
- tests/formats/renderers/year/holocene.test.js — verify test file exists and is correct
- tests/formats/renderers/year/gregorian.test.js — verify test file exists and is correct
- tests/formats/renderers/year/meghalayan.test.js — verify test file exists and is correct
- tests/formats/renderers/year/custom.test.js — verify test file exists and is correct
- tests/formats/renderers/date/gregorian.test.js — verify test file exists and is correct
- tests/formats/renderers/date/chinese.test.js — verify test file exists and is correct
- tests/formats/renderers/date/longitudinal.test.js — verify test file exists and is correct
- tests/formats/renderers/date/year-boundary.test.js — verify test file exists and is correct
</read_first>
<action>
Run the renderer test suite and the full test suite to confirm all tests pass.

Step 1: Run only the new renderer tests:
```sh
npx vitest run tests/formats/renderers/
```
Expected: all 8 test files pass, 0 failures.

Step 2: Run the full test suite to check for regressions:
```sh
npm test
```
Expected: all tests pass, including pre-existing tests in:
- `tests/pure/` (holocene, meghalayan, etc.)
- `tests/formats/` (config, registry)
- `tests/integration/` (composer, alarm-flow)
- `tests/alarms/`, `tests/converters/`, `tests/location/`, `tests/visual/`

If any test fails in Step 2 that was NOT created in Phase 11:
1. Read the failing test and the file it imports
2. Determine if the Phase 11 renderer changes broke an existing behavior
3. Fix the regression without changing test assertions — only fix the implementation if needed

Common regression risk: `tests/formats/registry.test.js` test at line 47-51 calls `yearRenderer({ holocene: 12026 })` and asserts `typeof result === 'string'`. This should still pass since the holocene renderer still returns a string.
</action>
<acceptance_criteria>
- `npx vitest run tests/formats/renderers/` exits 0 with all tests passing
- `npm test` exits 0 with all existing tests still passing
- `npx vitest run tests/formats/renderers/year/holocene.test.js` exits 0
- `npx vitest run tests/formats/renderers/year/gregorian.test.js` exits 0
- `npx vitest run tests/formats/renderers/year/meghalayan.test.js` exits 0
- `npx vitest run tests/formats/renderers/year/custom.test.js` exits 0
- `npx vitest run tests/formats/renderers/date/gregorian.test.js` exits 0
- `npx vitest run tests/formats/renderers/date/chinese.test.js` exits 0
- `npx vitest run tests/formats/renderers/date/longitudinal.test.js` exits 0
- `npx vitest run tests/formats/renderers/date/year-boundary.test.js` exits 0
</acceptance_criteria>
</task>

## Verification

```sh
# Run all renderer tests
npx vitest run tests/formats/renderers/

# Run full suite for regression check
npm test
```

Both commands must exit 0.

## must_haves

- All 8 renderer test files pass with 0 failures
- `tests/formats/renderers/date/year-boundary.test.js` (DATE-04 integration tests) all pass
- The existing `tests/formats/registry.test.js` still passes (no regression from renderer changes)
- `npm test` exits 0 (no regressions in any existing test)
- The following specific assertions all pass:
  - `render({ holocene: 12026 })` → `'H12026'` (holocene renderer)
  - `render({ effectiveYear: 2025 })` → `'H11725'` (holocene with effectiveYear)
  - `render({ meghalayan: { stage: 'northgrippian', year: 3327, label: 'Nrg 3327' } })` → `'Ngp3327'` (meghalayan renderer, Ngp not Nrg)
  - `render({ lunisolar: { month: 6, day: 1, isLeap: true } })` → `'MX D1'` (chinese, leap = MX not M6X)
  - `render({ solarLongitude: 'SL024', lunarPhase: 'LP180' })` → `'☉ 024° ☽ 180°'` (longitudinal, no SL/LP prefix in output)
  - `renderHolocene({ effectiveYear: 2025 })` → `'H11725'` (year boundary DATE-04)
  - `renderMeghalayan({ effectiveYear: 2026 })` → `'Mgh4226'` (year boundary DATE-04)
